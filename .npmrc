# Ensure scoped packages publish to GitHub Packages (not npmjs)
@navikt:registry=https://npm.pkg.github.com

# Auth for GitHub Packages (provided by GitHub Actions)
//npm.pkg.github.com/:_authToken=${NODE_AUTH_TOKEN}

always-auth=true
